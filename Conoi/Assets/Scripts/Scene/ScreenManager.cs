using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ScreenManager : MonoBehaviour
{
    public void ShowScreen(Sprite image, Sprite imageText, Color color)
    {
        gameObject.SetActive(true);
    }
}
